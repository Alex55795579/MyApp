<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.user.myapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DrawerActivities.News" />
        <activity android:name=".DrawerActivities.AboutUs" />
        <activity android:name=".DrawerActivities.Help" />
        <activity android:name=".DrawerActivities.Contacts" />
        <activity android:name=".DrawerActivities.Images" />
        <activity android:name=".DrawerActivities.Videos"></activity>
    </application>

</manifest>